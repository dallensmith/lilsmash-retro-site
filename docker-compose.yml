version: '3.8'

services:
  smashshack:
    build: .
    container_name: smashshack_website
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
